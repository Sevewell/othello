version: '3'
services: 
    engine:
        build: ./engine
        ports: 
            - "8080:8080"
        volumes:
            - ./engine/info:/usr/src/app/info
    interface:
        build: ./interface
        ports: 
            - "80:3000"
        